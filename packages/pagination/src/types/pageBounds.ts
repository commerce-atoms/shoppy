/**
 * Page bounds for array slicing operations.
 */
export interface PageBounds {
  /**
   * Start index (0-based, inclusive)
   */
  start: number;

  /**
   * End index (0-based, exclusive)
   */
  end: number;
}

